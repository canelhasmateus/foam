{
  "name": "foam-domain",
  "version": "0.0.1",
  "license": "MIT",
  "module": "out/*.js",
  "types": "out/index.d.ts",
  "scripts": {
    "build": "tsc -p ./",
    "test": "jest --testMatch \"**/*.test.ts\"",
    "bench": "jest  --testMatch  \"**/*.bench.ts\" --silent false"
  },
  "devDependencies": {
    "@types/jest": "^29.2.5",
    "jest": "^29.3.1",
    "kelonio": "^0.8.0",
    "ts-jest": "^29.0.3",
    "typescript": "^4.9.4"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
